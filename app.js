 





 console.log(`
    name: ${name}
    roll:${roll}
    class:${className}
===============================================
subject         marks             Gpa                        Grade
bangla           ${ban}        ${ stResult(ban) }      
English         ${eng}          ${ stResult(eng)}
Math             ${math}        ${ stResult(math)}       
physics         ${physics}      ${ stResult(physics)}
chemistry       ${che}          ${ stResult(che)}
farmer          ${farmer}       ${ stResult(farmer)}
 `);